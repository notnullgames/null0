include "null0.h"

export fn load() {
    clear(BLUE);
    draw_circle(100, 100, 50, RED);
    draw_text(FONT_DEFAULT, "hello from Zen-C", 170, 92, WHITE);
}

// callbacks (optional - implement as needed)
//
//   export fn update() {}
