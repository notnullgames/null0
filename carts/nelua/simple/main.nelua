require 'null0'

local function load() <cexport'load'>
  clear(BLUE)
  draw_circle(100, 100, 50, RED)
  draw_text(FONT_DEFAULT, 'hello from Nelua', 170, 92, WHITE)
end

-- callbacks (optional - implement as needed)
--
--   local function update() <cexport'update'> end
